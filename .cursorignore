# ----------- SDK中的非源码目录-----------
compiletools/
doc/
IPU/
third/
Tools/
# ============================================
# 二进制与编译产物 (AI 无法阅读这些，必须忽略)
# ============================================
*.o
*.a
*.so
*.so.[0-9]*
*.elf
*.bin
*.hex
*.ko
*.cmd
*.su
*.d
*.builtin
*.order
# 即使你在 git 中保留了特定的 .mod.c，通常这些是自动生成的，
# AI 只需要看对应的 .c 源码，不需要看 .mod.c
*.mod.c 

# 压缩包与大文件
*.zip
*.tar
*.gz
*.xz
*.iso

# ============================================
# 构建与输出目录
# ============================================
/build/
/bin/
/obj/
/out/
# 嵌入式系统生成目录 (通常包含大量重复或二进制文件)
/sysroot/
/rootfs/
/images/

# ============================================
# 配置文件与元数据
# ============================================
# 编译数据库 (虽然是文本，但文件极大且对 AI 理解代码逻辑帮助有限，建议忽略)
compile_commands.json

# C/C++ 索引文件 (如果你用 cscope/ctags)
cscope.out
tags
GTAGS

# ============================================
# 临时文件与日志
# ============================================
*.log
*~
*.swp
.DS_Store

# ============================================
# 缓存与 IDE 配置
# ============================================
.ccache/
.cache/
.vscode/
.git/

# ============================================
# 第三方库/依赖 (可选)
# ============================================
# 如果某些第三方库目录特别大，且你不需要 AI 修改里面的代码，
# 建议将其加入，以节省 Token 并提高回答准确度。
# 例如：
# /code/project/release/chip/m6/sigma_common_libs/
